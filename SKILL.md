# Nekodex Skill Guide

Use this file for reusable project workflows, commands, and task-specific playbooks.

## Release Checklist

1. Run `pnpm typecheck`.
2. Run `pnpm lint`.
3. Run `pnpm test`.
4. Run `pnpm build`.
5. Publish with `npm publish --access public` when `NPM_TOKEN` is configured.
