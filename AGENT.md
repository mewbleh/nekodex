# Nekodex Agent Guide

- Inspect relevant files before editing.
- Keep changes focused and verify with `pnpm typecheck`, `pnpm lint`, and `pnpm test`.
- Avoid committing credentials, generated dependencies, or build output.
- Prefer project-local conventions over new abstractions.
